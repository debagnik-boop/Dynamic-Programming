#include<iostream>
#include<unordered_map>
using namespace std;
int solveTd(int n,unordered_map<int,int> memo){
    if(memo.find(n)!=memo.end()) return memo.at(n);
    else if(n==1) return 0;
    else{
        int div_by_2=
    }
}

